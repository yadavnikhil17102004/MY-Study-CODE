#include <stdio.h>

int main()
{
    int x, y, z;
    printf("Enter the value of x: \n");
    scanf("%d", &x);
    printf("Enter the value of y\n");

    scanf("%d", &y);
    printf("Enter the value of z\n");
    scanf("%d", &z);

    /*addting of x y */
    printf("\nThe sum of %d + %d is =%d ", x, y, x + y); // 10+
}
